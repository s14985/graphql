scalar DateTime

type Order {
	id: ID!
	status: Status
	dateCreated: DateTime
	productOrders: [ProductOrder]
	user: User
}

input OrderInput {
	id: ID
	status: Status
}

enum Status {
	CREATED
	FINISHED
}

extend type Mutation {
	newOrder(productOrders: [NewProductOrderInput!]!): Order
	editOrder(id: ID!, productOrders: [EditProductOrderInput]!): Order
}

extend type Query {
	findAllOrders: [Order]!
}
